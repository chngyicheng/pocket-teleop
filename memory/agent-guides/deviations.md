# 已知偏差（後續工作仍相關）

| 偏差 | 位置 | 受納理由 |
|---|---|---|
| 一切建置須 `--network=host` | `docker-compose.yml`、建置指令 | Pi5 於 Docker bridge 網絡中不能解析 DNS — 省略則建置靜默失敗 |
| `#define ASIO_STANDALONE` 禁用 | `teleop_server.hpp` 及一切新服務器 WebSocket 代碼 | Dockerfile 安裝 `libboost-system-dev`（Boost ASIO）；standalone ASIO（`libasio-dev`）未安裝 |
| `docker-compose.yml` 環境值須加引號 | `docker-compose.yml` | Docker Compose v2.35+ 無法解析 `${VAR:?msg: with colon}` 中之未加引號 YAML 字符串 |
| `moduleResolution: node16`（非 `bundler`） | `web-client/tsconfig.json` | `bundler` 允許無擴展名導入，於 nginx 服務之 ES 模塊中 404；`node16` 強制 `.js` 擴展名 |
| `module: Node16`（非 `ESNext`） | `web-client/tsconfig.json` | TypeScript 5 拒絕 `module: ESNext` + `moduleResolution: node16`，報 TS5110 |
| `node:22-slim`（非 `node:20-slim`） | `web-client/Dockerfile.webclient` | Node 20 無原生 `WebSocket` 全局對象；連接嘗試靜默失敗 |
| `navigator` 守衛須查 `getGamepads`，非僅 `navigator` | `web-client/src/gamepad_handler.ts` | Node 22 全局定義 `navigator` 但無 `getGamepads`；裸 `typeof navigator` 守衛崩潰 |
| `TeleopClient` 從 `onError` 和 `onclose` 雙觸 retry | `web-client/src/teleop_client.ts` | Node.js 22 原生 WebSocket 對被拒連接僅觸 `onerror`；`retryPending` 守衛防止瀏覽器雙觸時重複調度 |
| `Touch` 構造函數在測試中 shimmed；`jsdom` 加入 devDeps | `web-client/test/touch_joystick.test.ts`、`web-client/package.json` | jsdom 24 暴露 `TouchEvent` 但非 `Touch` 全局構造函數；shim 定義最小類滿足構造調用 |
| `.drawer-page[hidden] { display: none }` 與 `[hidden]` 並用 | `web-client/index.html` | 作者 CSS `.drawer-page { display: flex }` 通過層疊覆蓋 UA `[hidden] { display: none }`；複合選擇器優先級更高，恢復正確行為 |
| `TouchJoystick` 用文檔級 Pointer Event 監聽器，無 `setPointerCapture` | `web-client/src/touch_joystick.ts` | Brave 在 `setPointerCapture` 啟用時將第二指的 `pointerdown` 路由至捕獲元素，損壞第二操縱桿的原點。修復：在 `document` 監聽；用 `e.target` 路由各 `pointerdown` 至正確區域；`_activeTouchIds` 模塊級集合防止瀏覽器復用 ID 時兩區域認領同一 `pointerId`。`touch-action: none` 仍須於區域元素設置。 |
| `PointerEvent` 在測試中 shimmed；復用 jsdom shim 模式 | `web-client/test/touch_joystick.test.ts` | jsdom 24 不暴露 `PointerEvent` 全局構造函數；shim 鏡像早前 `Touch` shim |
| `style.display = ''` 不能顯示帶 CSS `display:none` 之元素 | `web-client/index.html` applyNamespace | 設內聯 display 為 '' 移除內聯覆蓋，CSS display:none 勝出；用 'block' 顯式顯示 |
| `Dockerfile` CMD 用 `${VAR:+-p name:=val}` 處理可選機器人參數 | `Dockerfile` CMD | ROS2 拒絕 `-p robot_name:=`（空值）；未加引號的含空格值引致詞分割；修復：`${ROBOT_NAME:+-p \"robot_name:=${ROBOT_NAME}\"}`——未設時跳過，設置時加引號；計劃僅更新 `teleop.launch.py` — Dockerfile 為計劃遺漏之獨立調用路徑 |
| `navigator.maxTouchPoints` 在 Brave 中返回 0（指紋保護） | `web-client/src/touch_joystick.ts` | Brave 無論設備均將 `maxTouchPoints` 置零；改用 `matchMedia('(pointer: coarse)')` 不被 Brave 抑制。Brave Android 已確認正常顯示提示。 |
| `vitest.config.ts` 加入顯式 `include: ['test/**/*.test.ts']` | `auth-server/vitest.config.ts` | Vitest 默認 glob 未發現 `test/` 子目錄中的測試；顯式配置無副作用 |
| `(FileStoreCreator as any)(session)` 強制轉型 | `auth-server/src/app.ts` | session-file-store 類型定義將導出聲明為類而非工廠；`as any` 強制轉型為社區公認做法 |
| `store.reapAsync` 調用在 `createApp` 中省略 | `auth-server/src/app.ts` | `reapAsync` 為可選維護；定期清理仍通過 `reapInterval: 3600` 運行；省略無正確性影響 |
| `(wsProxy as any).upgrade!` 非空斷言 | `auth-server/src/proxy.ts` | http-proxy-middleware v2 類型定義將 `upgrade` 標為可選，但構造函數始終賦值；外部審查後移除守衛 |
| `TELEOP_SERVER_URL` 默認 `http://` 非 `ws://` | `auth-server/src/index.ts` | http-proxy-middleware 需要 HTTP 目標 URL 用於 WebSocket 代理；`ws://` 引致協議錯誤 |
| `auth-server/Dockerfile.auth` 以應用所有權創建 `/data` | `auth-server/Dockerfile.auth` | 無顯式 `mkdir + chown`，卷掛載 `/data` 默認 root 所有，`app` 用戶不能寫入憑據 |
| `webclient-test` 通過 auth-server 代理路由 | `docker-compose.yml` | 集成測試走完整路徑（瀏覽器→auth-server→teleop-server），匹配生產拓撲；在 Task 8 中發現 |
| `Dockerfile`（C++ 服務器）移除 `token` 啟動參數 | `Dockerfile` | TELEOP_TOKEN 在 Task 7 退役後須移除 token 參數；Dockerfile CMD 為計劃遺漏之獨立調用路徑 |
| `.accordion-body[hidden] { display: none }` 必備 | `web-client/index.html` | 作者 CSS `.accordion-body { display: flex }` 覆蓋 UA `[hidden]` 屬性；複合選擇器恢復正確行為（與 `.drawer-page[hidden]` 同模式） |
| Eyeball SVG 在三文件中重複 | `login.html`、`change-password.html`、`index.html` | 各為獨立服務端渲染 HTML；共享提取不值複雜度；每份 SVG 約 500 字節 |
| Change-password 路由按上下文行為不同 | `auth-server/src/routes/auth.ts` | 強制首次登錄（mustChangePassword=true）保持 session 並重定向至 `/`；自願賬戶頁修改銷毀 session 並重定向至 `/auth/login` 強制重新認證 |
| `Cache-Control: no-store` 設於所有已認證代理響應 | `auth-server/src/app.ts` | 防止瀏覽器磁盤緩存和 bfcache（Chrome/Firefox）在登出後提供舊頁面；取捨：代理靜態資產無緩存——可接受，因 nginx 在生產中直接提供資產 |
| 賬戶頁表單用 `fetch()` 非原生 HTML POST | `web-client/index.html` | 原生表單 POST 將瀏覽器導航至純文本錯誤響應（如 `res.status(401).send('...')`）；fetch 允許在現有 `.form-error` 元素中內聯顯示錯誤 |
| Auth 在 `visibilitychange` 上的檢查每次切換標籤均觸發 fetch | `web-client/index.html` | `/auth/me` 為微小 JSON 響應（約 50 字節）；頻率受用戶操作限制（切換標籤、手機喚醒），非輪詢；取捨：每次可見性更改一次額外請求——對 session 安全可接受 |
| Docker healthcheck for auth-server 用內聯 `node -e` | `docker-compose.yml` | `node:22-slim` 基礎鏡像無 `wget` 或 `curl`；內聯 Node.js HTTP 請求零依賴 |
| Docker healthcheck for teleop-server 用 `bash /dev/tcp` | `docker-compose.yml` | ROS humble 基礎鏡像有 `bash` 無 HTTP 客戶端工具；`/dev/tcp` 為 bash 內置，無需額外依賴測試 TCP 連通性 |
| `TELEOP_SERVER_URL` 可從 `.env` 配置 | `docker-compose.yml` | `host-gateway`（解析 `host.docker.internal`）需 Docker >= 20.10；舊版 Docker 靜默失敗；URL 可通過 `.env` 覆蓋，用戶無需編輯 compose 即可替換 LAN IP |
| `fastrtps_profiles_observer.xml` 加入用於跨機器 ROS2 觀測 | `server/fastrtps_profiles_observer.xml` | 多播損壞之機器（`[Errno 19] No such device`）無法通過默認 SPDP 多播發現 ROS2 參與者；需要 `useBuiltinTransports=false` + `initialPeersList` 指向機器人 IP 的純單播配置；主服務器配置不變，因其已接受任何白名單接口上的單播 SPDP |
| `auth-server` 改為 `network_mode: host`；`webclient` 在迴環上暴露端口 18080 | `docker-compose.yml` | UFW（主機上啟用）通過 INPUT 鏈阻止從 Docker bridge 網絡到主機 bridge-gateway IP（`172.18.0.1`）的入站 TCP；`host.docker.internal` 映射到不同 bridge 上的 docker0（`172.17.0.1`），同樣被阻止；auth-server 到 teleop-server（主機網絡）的唯一可靠路徑是 host 模式網絡，兩者均見 `localhost:9091`；webclient 在 `127.0.0.1` 暴露端口 18080，auth-server 可代理而不跨 bridge 邊界；`webclient-test` 亦切換至 host 網絡以達 `localhost:8080` |
| `auth-server/src/index.ts` 的 `PORT` 環境變量和 `detectGateway()` 移除 | `auth-server/src/index.ts` | `PORT` 為 host 網絡部署在端口 8080 上而添加；`detectGateway()` 為 UFW 阻止 bridge→host 流量的變通方案，一旦使用 host 網絡則無需 |
| `WhepClient` 通過 mocked `RTCPeerConnection` shim 測試 | `web-client/test/whep_client.test.ts` | jsdom 24 無 `RTCPeerConnection`；shim 在測試文件中定義（與 `Touch` 和 `PointerEvent` shim 同模式）；13 個測試覆蓋 connect、retry、stop、back-off、onStream、onClose |
| `video-bridge` 通過 pytest 在純 pipeline 函數上測試 | `video-bridge/test_video_bridge.py` | 無硬件無法測試 GStreamer 管道；pipeline 字符串構建函數（`_compressed_pipeline`、`_raw_pipeline`、`_FORMAT_MAP`）為純函數，19 個 pytest 測試完整覆蓋 |
| `<img id="video-img">` 移除後以 `<img id="mjpeg-img">` 替換 | `web-client/index.html` | v0.11.0 重加 MJPEG 直連支持；新元素默認隱藏，由 `onMjpegUrl` 回調切換顯示 |
| `loadVideoUrl` / `saveVideoUrl` / `clearVideoUrl` 從 settings.ts 導入中移除 | `web-client/index.html` | MJPEG 路徑移除後不再需要；`settings.ts` 函數仍在源中供將來使用 |
| `vi.runAllMicrotasksAsync` 替換為 `flushPromises` 循環 | `web-client/test/whep_client.test.ts` | `vi.runAllMicrotasksAsync` 在 Vitest 2.x 加入；本項目用 Vitest 1.6.1；十個連續 `await Promise.resolve()` 調用可靠地刷新所有待處理微任務 |
| `monkeypatch.setattr(vb, 'MEDIAMTX_RTSP', ...)` 替代 `importlib.reload` | `video-bridge/test_video_bridge.py` | `importlib.reload` 就地重寫模塊字典，測試後不恢復；`monkeypatch.setattr` 干淨地修補和恢復模塊級常量 |
| `video-bridge-test` compose 服務運行 `python3 -m pytest`（非 `pytest`） | `docker-compose.yml`、`video-bridge/Dockerfile.video_bridge` | ROS Humble 基礎鏡像中 `pip3 install pytest` 將二進制放在非 `$PATH` 位置；`python3 -m pytest` 通過安裝模塊始終有效 |
| `/mediamtx-api` 前綴在代理層剝離至 `/v3` | `auth-server/src/app.ts` | 避免在公開 auth-server 暴露原始 `/v3` 路徑；在 `/video`（WHEP 媒體）和 `/mediamtx-api`（配置 API）之間清晰分離 |
| Body parsers 僅作用於 `/auth`（非全局） | `auth-server/src/app.ts` | 全局 `express.json()` 在代理管道之前消耗請求流；代理請求無限期掛起（永不結束）；限定於 `/auth` 使流對代理路由保持完整 |
| `pathRewrite: { '^/mediamtx-api': '/v3' }` 替代手動 `req.url` 變更 | `auth-server/src/app.ts`、`auth-server/src/proxy.ts` | http-proxy-middleware v2 在 `prepareProxyRequest` 中重置 `req.url = req.originalUrl`，丟棄任何先前變更；`pathRewrite` 在重置後運行，因此正確生效 |
| 診斷 PATCH 測試加入 auth-server 測試套件 | `auth-server/test/auth.test.ts` | 現有 `/mediamtx-api` 測試僅查「非 302」且未向 mock 傳遞 `mediaMtxApiUrl`——未能驗證任何事；新測試確認 PATCH 方法、`/v3` 路徑和 body 均正確到達 mock |
| `disabled` 模式發送 `{ source: 'publisher' }` 至 MediaMTX | `web-client/src/video_source.ts` | MediaMTX PATCH API 拒絕 `sourceRedirect` 字段（返回 400）；`publisher` 為有效值，停止任何主動拉流；video-bridge 未推流時 WhepClient 收到 404 並顯示佔位符 |
| 視頻源狀態存於 `localStorage` 並在加載時重新應用 | `web-client/src/video_source.ts` | MediaMTX 運行時配置易失（重啟即丟）；頁面加載時重新應用可修正漂移，無需添加服務端持久化層 |
| `VideoSourcePicker` 以 `fetchFn` 為構造器選項 | `web-client/src/video_source.ts` | 無需 `vi.stubGlobal` 副作用即可進行純 vitest 測試；與 `WhepClient` 回調相同的依賴注入模式 |
| `boundKeyUp` 即時觸發 twist（非輪詢驅動） | `web-client/src/keyboard_handler.ts` | 輪詢間隔（200ms）產生滑行窗口；key-up 處理器原子地重新計算並觸發更新後的 twist，無需複製輪詢邏輯 |
| 空格 e-stop 在 `activeElement` 為 input/textarea/select 時跳過 | `web-client/index.html` | 空格為文本域中的有效字符；標籤檢查防止在輸入 RTSP URL 或配置文件名時意外停止 |
| Gamepad calibration 分為提示 + 採樣兩個階段 | `web-client/index.html` | 原始單階段採樣在指令顯示後立即開始；用戶無時間在採樣開始前調整搖桿位置 |
| `validate()` 在 `VideoSourcePicker` 上，非 `buildMtxSource` | `web-client/src/video_source.ts` | `buildMtxSource` 為純數據函數；驗證為策略關注點，屬於有狀態類 |
| 延遲更新僅在閒置時（無主動駕駛） | `web-client/src/teleop_client.ts` | Keepalive ping 僅在 200ms 內無 twist 發送時觸發；主動駕駛時連續 twist 抑制 ping；此為正確取捨——閒置時的延遲比駕駛時更有用 |
| `WhepState` `'error'` 與 `'retrying'` 不同 | `web-client/src/whep_client.ts` | `onError` 觸發於觸發 retry 的瞬態流錯誤（如源不可用）；真正的 error 狀態保留給不重試的 fetch/網絡故障 |
| Odom 在服務端以 10 Hz 節流 | `server/src/teleop_node.cpp` | `/odom` 常以 50+ Hz 發布；每條消息通過 WebSocket 發送將飽和 Raspberry Pi 的上行鏈路；10 Hz 足夠顯示 |
| 航向以弧度發送；UI 轉換為度 | Protocol、`web-client/index.html` | 弧度為原生 ROS2 單位；UI 中轉換保持協議整潔，避免服務端度轉換的浮點精度損失 |
| Odom 面板在收到首條消息前隱藏 | `web-client/index.html` | 不發布 `/odom` 的機器人不應顯示陳舊或空白面板；默認隱藏防止混淆 |
| `broadcast()` 忽略 `ws_server_.send` 的 `error_code` | `server/src/teleop_server.cpp` | 客戶端斷開通過 `on_close` 將 `has_client_` 置 false；對陳舊 send 的靜默錯誤無害，避免競態條件檢查 |
| `showVideoStream()` 隱藏 `mjpegImg` 無單元測試 | `web-client/index.html` | 邏輯在 `<script type="module">` 內聯，vitest 無法導入；需 Playwright 等 e2e 工具方可覆蓋；用戶決策：人工測試（切換 MJPEG 源後等 WhepClient 重連確認無雙流疊加）|
| REC indicator chip 不渲於 MissionTablet main viewport | `web-client/src/views/MissionTablet.tsx` | Design `mission.jsx:309-317` 有「REC 02:14」紅色 blinking badge，本港省去；bridge 無 recording 訊號源，渲固定字串誤導；待後續若加 session-recording 役（計劃池內），再實接入 |
| Tablet layout 閾值 `min-width: 700px`（非 design 假設之 900+） | `web-client/src/App.tsx` | Samsung Fold 6 inner display CSS width 約 707 px（physical 1856 / DPR 2.625）；900 閾值致 fold 永留 phone layout；700 閾值令 Fold + iPad mini portrait 皆切 tablet；下行邊界仍排除 standard phone 寬（≤ 620 px landscape） |
| `SettingsDrawer` 自左側滑、起於 top bar 之下、tap-outside 自閉（design 無對應 drawer 元素） | `web-client/src/components/SettingsDrawer.tsx`、`App.tsx` | 本港自加 drawer 容遊戲手柄/視頻源/namespace 設置，design 無此面板；用戶硬件測試後定案：burger 於左故 drawer 亦自左滑（方向一致），burger 再按可 toggle 關閉；drawer 起於 `topOffset`（tablet/landscape 44px、portrait 36px）之下，不遮 top bar 之 E-STOP；backdrop scrim（僅 open 時渲，起於 top bar 下）點擊即閉；樣式改用 Mission palette（surface `#14171e`、amber `#f0a92a`、JetBrains Mono 標題），與全 app 一致（先前藍 `#3b82f6`/`#0b0d12` 為走樣）|
| top-bar `UP`/`BAT`/`SIG` 顯 `—`（不渲假值） | `web-client/src/views/MissionTablet.tsx`、`MissionControl.tsx` | BUG 5：原硬編碼 `03:24:18`/`78%`/`-58dBm` 為虛構值，於 teleop 安全 UI 上誤導操作員；現無真實電量/訊號/uptime telemetry 來源（battery 於計劃池有 backlog），故顯 `—` 表「無資料」，待真源接入；LAT 為真值不在此列 |
| STREAM `fps`/`res` 取自 `RTCPeerConnection.getStats()` inbound-rtp video | `web-client/src/whep_client.ts`、`useWhepStream.ts`、`views/MissionTablet.tsx` | BUG 5：原硬編碼 `30.1`/`1280×720` 既假且錯（管線實為 15fps/1920×1080）；`WhepClient` 於 live 後每 1s poll `getStats`，取 `framesPerSecond`/`frameWidth`/`frameHeight` 經 `onStats` 上拋，hook 存於 `stats: VideoStats|null`；首 sample 前顯 `—` |
| STREAM `src`='WebRTC'、`codec`='H.264' 仍為靜態字串 | `web-client/src/views/MissionTablet.tsx` | 二者為當前管線真實值（WebRTC 傳輸、H.264 編碼），靜態但準確；已加註；如日後管線可變，再由 `getStats` codec 報告動態接入 |
| Phone-portrait 不轉可摺側欄，仍存浮層 overlay | `web-client/src/views/MissionControl.tsx` | 操作者決：landscape 與 tablet 改為真側欄，portrait 幅過窄不容左右欄；portrait 保原浮層佈局，以 regression test（layout='phone-portrait' 時無 `rail-tab-*`、浮層 VelBars/MiniMap 仍在）護之 |
| Landscape rail 內容以 inline div 呈（非 `SidePanel`/`DataRow` 組件） | `web-client/src/views/MissionControl.tsx` | `MissionControl` 原為浮層佈局，從未 import `SidePanel`/`DataRow`（彼為 `MissionTablet` 之 helper）；landscape rail 重構時就地 inline 等效 markup，免跨檔引組件徒增 churn；`MissionTablet` 右 rail 則由 controller DRY-復用既有 `SidePanel` |
| 可摺役之 subagent 不更 AGENTS.md，handover 由 controller 末了統一更 | 流程（subagent-driven-development） | T1/T2/T3 三 Haiku 序行於同一 worktree、共一 git index；若各自改 AGENTS.md 同列易致 index 衝突；故令各 subagent 僅 commit 己之碼+測，controller 於 T4 統一更 handover 表 + Milestones + Document index，仍與末一 commit 同 |
| `CollapsibleRail` toggle tab 寬 20px、`zIndex 5` 疊於 rail 內緣之上 | `web-client/src/components/CollapsibleRail.tsx` | tab 為透明 overlay 居內緣（left rail 貼右、right rail 貼左），疊 content 最內 20px；於 jsdom 測無礙，真機視覺上 chevron 落於 rail 邊；content padding 足，重疊不掩關鍵字；簡為要，未另闢 tab 專屬欄寬 |
| 字型用 latin-subset **variable** woff2 + weight-range `@font-face`（非各 weight 一檔） | `web-client/public/fonts/`、`web-client/src/index.css`、`auth-server/public/fonts/` | Google Fonts latin subset 之 Inter／JetBrains Mono 皆 variable font，一檔涵全 weight 範圍；故 `@font-face` 用 `font-weight: 400 700`（Inter）／`400 500`（mono）單面指同一 woff2，免冗載多檔；offline 自足，無外網依賴 |
| `Dockerfile.auth` runtime stage 增 `COPY public ./public` | `auth-server/Dockerfile.auth` | 新 `/auth-static` 路由由 `express.static(path.join(__dirname,'../public'))` 服字型；runtime stage 原僅 copy `dist`+`views`，prod 將 404 字型；vitest 跑 src 故未顯此漏，controller 建鏡像 inspect 方捕；補 COPY 令 `/app/public/fonts` 隨鏡像出 |
| auth `__dirname` 於 `createApp` 內以 `fileURLToPath(import.meta.url)` 算 | `auth-server/src/app.ts` | app.ts 原無 path/url import；ESM 無 `__dirname` 全局；沿 `routes/auth.ts` 既有同模式；`../public` 自 src（或 dist）解至 `auth-server/public` |
| `enforceDefaultCredentialChange` 在持久 volume 留預設態時每啟動重設 `mustChangePassword` | `auth-server/src/credentials.ts`、`index.ts` | `initCredentials` 僅檔不存時寫 flag，持久 `auth-data` volume 留 `mustChangePassword:false` 致 admin/admin 直入；啟動檢若 env 預設密碼仍 verify 則強制改密 true；操作者改密後不再觸（極端：刻意設密碼=env 值則每啟被迫再改，屬不安全用法，可接受） |
| Haiku 子代理 cwd 釘於主 repo 而非 worktree；controller 以 `git diff｜git apply` 轉移 | 流程（subagent-driven-development） | T-B 子代理之改落於 `/home/chngyicheng/pocket-teleop`（主 repo）非 worktree；疑子代理 launch 時 cwd 釘原 session dir；controller 自主 repo `git diff` 出 patch、`git apply` 入 worktree、復原主 repo；教訓：自 worktree 派子代理後須驗主 repo 與 worktree 二處 `git status` |
| `VideoSignalOverlay` 置於 `shared.tsx`（plan 乙役 scope 僅列二視圖） | `web-client/src/components/shared.tsx`、`MissionControl.tsx`、`MissionTablet.tsx` | NO SIGNAL overlay 於三視頻區（tablet main、landscape main、portrait 浮層）皆同；置於 `shared.tsx` 一處 export、三處 import，免重複三份 markup；DRY 勝於嚴守 plan 列檔；視圖僅加一行 `<VideoSignalOverlay state={stream.state}/>` |
| 視頻 dynamic-import code-split 僅省約 1 KB（191031→189988 B initial JS） | `web-client/src/hooks/useWhepStream.ts`、build `dist/assets/whep_client-*.js` | plan 已預判：WhepClient 為純 WebRTC 邏輯，相對 React ~130KB 為小頭；`whep_client` 切出獨立 2.95 KB chunk（gzip 1.14 KB），initial 僅縮 ~1 KB；**主效益在延 `start()` 釋主執行緒**（首繪後 `requestIdleCallback`／`setTimeout` 排程），非 bundle 縮量；誠實記實測差，不誇大 |
| UI-ready perf beacon：client `POST /perf`、auth-server 記 `[perf]` log（plan 無此項） | `web-client/src/perf_beacon.ts`、`main.tsx`、`auth-server/src/app.ts` | 操作者請求量「請求 UI → 載畢」之真實耗時；nginx access log 僅見 request 到達、不見 client 首繪。故加 beacon：React mount 後 double-rAF（待首繪 commit）以 `sendBeacon`（fallback keepalive-fetch）送 Navigation/Paint Timing（`readyMs`≈首繪、`fcpMs`、`domContentLoadedMs`、`responseEndMs`、`ua`）至新 `POST /perf`。route 置於 auth-redirect 之前（unauthenticated，body parser 僅限本 route），log `[perf] <iso> <json>`；`docker logs … | grep perf` 即見每次載入之真實 readyMs。best-effort，失敗不擾 app |
| 即時 boot splash 寫於 `index.html`（非 React component） | `web-client/index.html` | 操作者報登入後白屏；量得真因為 190KB bundle 下載+執行於首繪前阻塞（`<script type=module>` 阻 DOMContentLoaded）。React splash 無用——其須俟 bundle 執行畢方現。故以手寫 `#boot-splash` markup + critical inline `<style>`（Mission 暗 `#0c0e12`、amber spinner、JetBrains Mono）置於 `index.html`，HTML 解析即繪（~0.6s），無待 bundle/index.css；React `createRoot` 首 commit 覆 `#app` 子節點而除之。html/body 亦 inline 設暗底，免 index.css 載前白閃 |
| nginx gzip 開、hashed `/assets/` immutable cache（新 `web-client/nginx.conf`） | `web-client/nginx.conf`、`Dockerfile.webclient` | 診斷 beacon 證白屏真因：bundle build 時 gzip 為 ~58KB，然 wire 出 186KB——`nginx:alpine` 預設 gzip **off** 且 image 無 custom conf，JS 未壓即送。慢且變動之 robot-AP link 上此 3x bloat 為首要耗（wire 1.7s 順境、~7s 逆境）。JS 執行僅 ~20ms，非瓶頸。新 `nginx.conf`：`gzip on`（JS/CSS/JSON/SVG）+ `/assets/` immutable cache（repeat load 免再下載）+ `index.html` no-cache（deploy 即生效），`COPY` 至 `/etc/nginx/conf.d/default.conf`。驗於 rebuilt image：bundle 出 `Content-Encoding: gzip`（186→~57KB）。非 Preact/React.lazy 之 app-weight 問題，乃 serving-layer config |
| UI-ready perf beacon 增 bundle resource-timing 欄（`bundleMs`/`bundleTransferMs`/`bundleKB`） | `web-client/src/perf_beacon.ts` | 為辨白屏屬下載抑或執行，beacon collectPerf 取 `/assets/index-*.js` 之 `PerformanceResourceTiming`：`bundleMs`=responseEnd−startTime、`bundleTransferMs`=responseEnd−requestStart、`bundleKB`=encodedBodySize/1024。一次真機載入即定案（transfer 1.7s、execute ~20ms、wire 186KB → 下載/未壓 bound），導向 gzip 修而非 Preact |
| `/ws` upgrade 須 session 鑑權；`makeWsUpgradeHandler` 增可選 `sessionMiddleware` 參，無則 fail-closed（401 + socket destroy） | `auth-server/src/proxy.ts`、`app.ts`、`index.ts` | H1 adversarial：原 upgrade handler 直 proxy 不驗 session，任 LAN client 皆可開 `/ws` 駕機器人。teleop socket 為控制通道，須與 HTTP 路由同等鑑權。express-session 中介經 `app.set('sessionMiddleware', …)` 外露，`index.ts` 傳入 handler；upgrade req 上跑該中介（res 以 minimal stub 充，僅載不寫 session），唯 `req.session.userId` 在者方 proxy。無中介時（如測試僅傳 target）一律拒——fail-closed 勝 fail-open |
| pong-timeout adversarial 測由 `vi.spyOn(client,'connect')` 改寫為 `vi.mock('../src/connection.js')` 標準 harness | `web-client/test/teleop_client_pong_timeout.adversarial.test.ts` | 原測 mock 整個 `client.connect`，致 `startKeepalive` 永不執行——keepalive timer 不生，無論實作如何 onClose 皆不觸，結構上不可能轉綠；且其 `connection.onMessage` 於裸 mock 物件上為 undefined，pong 永不達 client。改用 continuous-publish 測既有之 `vi.mock(connection)` 範式：真 `connect` 跑、`startKeepalive` 真起、captured `send` 可令測按需回 pong。斷言意圖不變（逾時 onClose／無 pong 不報延遲／有 pong 則健康）。`afterEach` 用 `clearAllMocks` 非 `restoreAllMocks`（後者會清 `vi.mock` factory 之 mockImplementation，破壞次 test 之 Connection mock） |
| `handlePongTimeout` 主動觸 `onClose(4000,'pong timeout')` 兼排程 reconnect（onClose 慣為 intentional-only 之終態） | `web-client/src/teleop_client.ts` | 連續 `maxMissedPongs`（默認 3）次 ping 無 pong 回 → 視 socket 為 zombie（open 而死）。為令操作員知連線已失而非默駛陳舊流，明呼 `options.onClose` 通報；同時（非 intentional 且 retry 未排）排 `scheduleRetry` 自復。語義上 onClose 兼通報＋自動重連，雖異於「終態」常規，於 zombie 偵測為正確取捨；既存 onClose handler 之 normal-close 仍靜默重連不呼 options.onClose，二路不相犯 |
| 病一（持桿反停）勘果：**client republish 已穩，非 client 之咎** | `web-client/src/teleop_client.ts`、`test/teleop_client_continuous_publish.test.ts` | trophy 測證：持非零桿，`startPublisher` 以 20 Hz 無窮 republish `repeatTwist`，永不自歸零；鬆桿方 `STOP_REPEATS`（10×50ms）後靜默。故持桿而停之真因疑在 **robot base controller 之 cmd_vel timeout**（須此 20 Hz 流維持，已具）或實地 republish 未達網。**待操作者於機器人端驗**：持桿時 wire 上 twist 之頻（應 ~20 Hz）與值（應穩定非零）。input-shaping deadzone 另解「鬆桿微值致蠕行」之亞病 |
| 硬件待驗三事（gamepad 改良）皆俟操作者實機校 | `web-client/src/gamepad_profiles.ts`、`gamepad_handler.ts` | (1) **軸 invert 方向**（務三）：az/ly/lx 之 `invert:true` 為沿用初值，未經實機；操作者於 DevTools 觀 "Gamepad detected" 後校，方向反則翻對應 `invert`。(2) **GameSir id pattern** `/gamesir/i` 未證；連 G8+ 觀 console `gp.id` 補正。(3) **LB = button index 4**（務五）：標準 mapping 為 4，個別控制器或異；estop 誤觸或不應則校 `buttons.estop` index（LT trigger=6，勿混）。**DualShock profile 之軸序與 estop button 皆未動**，留另驗 |
| 務四 SPEED 控件 MissionControl 置 landscape rail **與** portrait overlay 二處（plan 僅列左 rail） | `web-client/src/views/MissionControl.tsx` | plan §務四定位左 rail（landscape STREAM rail，VELOCITY 之下）——已遵。phone-portrait 無側欄，若僅 landscape 則 portrait 操作者不得調速；故 portrait overlay（VELOCITY 浮層內）亦置同一 `SpeedStepper`。二處共用 bridge state，加置為補全非減損；測各驗其位 |
| gamepad 改良六務以**串接 worktree**行，末了一併 merge 入 main | 流程（subagent-driven-development） | 操作者令「merge to main at the end」：務一/四/五同改 `teleop_client.ts`、務二/四同改 views，須序列累積。務A/務一 merge 入 main 後，務三起以 `feat/gp-*` 串接（各自前務 branch 分枝），handover/Test baseline 於末務統一更（免逐務改同列致衝突），全鏈 merge 前經全套測（webclient 451 綠） |
| 病一真因勘定＋治：`GamepadHandler` 改以 **requestAnimationFrame** 驅 poll（非 setInterval） | `web-client/src/gamepad_handler.ts` | 操作者實測（持桿一發即停、唯動桿方續、釋桿見 spurt）證 React 版持桿不續。真因：Chrome 唯於 rAF/compositor loop 刷 `getGamepads()` 快照；React app 無常駐 render loop，故 setInterval 讀持桿得 stale（neutral），加 BUG1 zero-burst 將單一 stale-0 latch 成 500ms 零＋靜默。改 start() 以 rAF loop 驅 poll（throttle ~20Hz，無 rAF 則 setInterval 退路），持桿讀新值、續發。v0.10.0（vanilla）無 rAF 卻可，疑彼時測於 desktop 或別境；rAF 為 Gamepad API 正法 |
| HUD 速度顯示取 active source 並示**實發 cmd_vel**（shaped × cap），非 raw 桿值 | `web-client/src/views/MissionControl.tsx`、`MissionTablet.tsx` | 操作者報「不知所發何值」「設 0.5 仍顯 1.0」。原 VelBars/底部 V·ω 僅取 touch 之 raw `lx/ly/az`（gamepad 不更、未 shape、未 scale）。今 `dispLx = gamepadActive ? gamepadTwist : lx`（gamepadTwist 已 shaped），VelBars 取之；MissionTablet 底部 V·ω 改 `hypot(disp)×maxLinear` / `disp.az×maxAngular`；MissionControl SPEED 區加 PUB readout 同。clamp 本就施於 sender（`sendScaledTwist`），顯示是缺口非 clamp 失效 |
| 左 rail 重排 VELOCITY→SPEED→ODOMETRY→VIDEO（video 資訊置底）；drawer 留 z1 不抬 | `web-client/src/views/MissionControl.tsx`、`MissionTablet.tsx`、`components/CollapsibleRail.tsx` | 操作者拒「抬整 drawer 之 z」（z 抬令 joystick 之上之 rail 全蓋）。改以**物理重排**：SPEED 移近 rail 頂，遠離底角 joystick，故 ± 不被 joystick 觸區所掩，無須 z hack。CollapsibleRail panel 復原 z1。portrait 無 video panel 可移（VELOCITY+SPEED 本在頂），overlay z 亦復原 |
| 長條手機橫向（aspect ≥ 17/10）用較小 joystick（150/90/38，不裁切）；Fold 兩向皆全 200 | `web-client/src/views/MissionTablet.tsx` | 寬幅長機橫向 width≥700 入 tablet layout 然 height 短，底角 joystick 蓋側 rail、阻其 touch-scroll。曾試 `overflow:hidden` 裁 tap 區，然裁切 base/knob 頂（slice）且誤及 Fold。改：依 `matchMedia('(min-aspect-ratio: 17/10)')` 唯長機橫向縮 joystick（size 150 < rail 高 → rail 可滾於其上），不裁切故無 slice；Fold（aspect <1.7）兩向留 200/120/50。matchMedia 加 guard 防無 DOM 之測境 |
| Brave gamepad 不可讀 — Brave fingerprinting shield 阻/farble `getGamepads()` | 外部（瀏覽器設定，非 repo） | 操作者報 Chrome 可、Brave 不顯 gamepad。Brave「Block Fingerprinting」shield 蓄意 farble/block Gamepad API，無 app 端解法。記 TROUBLESHOOTING.md：Shields → Fingerprinting → Standard 或關，或用 Chrome |
| minimap 螢向諸式待操作者目驗（hardware-verify） | `web-client/src/map_render.ts` | mapToScreenTransform／scanToScreenPoints 之正負已由單測 vector 鎖死（前行→上、左物→左等），且 live smoke 證 pose frame=map、scan 120 點達 WS。然「式自洽」非「式合世」——須實機目驗：robot 前行→圖向下流（robot 居中定）、左旋→圖順時針轉、scan 點落實牆之向。反者翻 map_render 對應符號（單測 vector 同步改） |
| scan 唯 yaw 校正，translation offset 忽略 | `server/src/teleop_node.cpp` | scan 以極座標（angle_min+ranges）過 wire，translation 無法施於極式而不展點列。lidar 距 base_link 偏置常 <0.2 m，minimap 尺度（~10 m 視窗於 ≤200 px）下誤差 <2 px。倒裝 lidar（roll≈π）此近似破——實機 scan 鏡像則於 SLAM 端正之或來日擴 protocol 為點列 |
| `TeleopServer::broadcast` 返 `bool`（API 變） | `server/include/teleop_server.hpp`、`teleop_node.cpp` | map 為 latch 型資料（odom/pose 流式，丟之無妨；map 丟之則 client 至多 5 s 盲）。原 void broadcast 致「發於無人而記為已發」——client 於 SLAM 更新間連入者永不得圖。今唯真送達方清 dirty 旗，次 tick 重試。odom/pose/scan 呼處棄返值如故 |
| 務三 subagent 於 main repo 作業且自創 hex RLE codec，controller 救治 | 流程（subagent-driven-development） | Haiku cwd 釘於 main repo（既知 gotcha #1），於 main（無務A 之 branch）重造 protocol/map_codec——其 RLE 為「hex pair」式，與 server 之 `u3f2o1` token 式**不合**，其測唯證己式。救治：bridge/client 接線 diff（與 codec 式無涉）轉移入 worktree，重造之 codec/protocol 盡棄（務A 版為正），測 fixture 改正式後全綠。教訓：凡 subagent 報「新增」既存之檔，即疑其樹其枝 |
| TLS 二模式俟操作者實境驗（live-verify pending） | `Caddyfile`、`docker-compose.yml`、`README.md` | compose config／caddy validate／cookie 單測皆綠，然未經實境：(1) self-signed 模式須 phone 信 root CA 後驗 WS upgrade＋video 過 https；(2) ACME 模式須公網域＋80/443 通，無實域可測。LAN hostname 而 email 設者 ACME 必敗——README 表已戒。驗法：README「Enabling HTTPS」節 |
| rate limit 手書 tracker，棄 plan 所定 express-rate-limit；429 出簡頁非 login 頁 | `auth-server/src/rate_limit.ts`、`routes/auth.ts` | login 失敗乃 302 redirect（`?error=1`），與成功 302 同 status——express-rate-limit 之成敗計以 status 斷，於此全廢，須 requestWasSuccessful 迂迴；handler 顯式 `recordFailure` 直截（~70 行，零新依賴）。429 體為內嵌簡頁附 retry 提示——login.html 靜態，無 templating 可示「稍後再試」，redirect 則失 429 status 之約 |
| idle timeout 之 activity 棄 plan 所定「WS message 視為活動」，改 client 端真人輸入 heartbeat | `auth-server/src/app.ts`、`web-client/src/hooks/useSessionStatus.ts` | client keepalive 每 ~200ms 發 ping over WS——凡 WS 流量計活動則 tab 開即永不逾時，feature 形同虛設。改：activity＝authenticated HTTP request（`GET /auth/session-status` 除外，否則 poll 自延壽）；真人輸入（pointerdown/keydown/gamepad inputSource）由 client 以 throttled（≥60s）`POST /auth/heartbeat` 上報。持桿不放者 active 信號無 transition，故 hook 另以 interval 重查——駕駛中 session 不滅 |
| WS mid-connection 逾時殺以手書 4001 close frame＋per-connection store 輪詢 | `auth-server/src/proxy.ts` | http-proxy-middleware 對 upgrade 後之 socket 唯 pipe raw TCP，無 ws API 可發 close；故 `buildWsCloseFrame`（0x88、payload len、code BE、reason ASCII，server→client 無 mask）直寫 socket。session 逾時偵測：每 60s `store.get(sid)` 重讀（session 滅或 lastActivity 逾期 → 殺）；無 lastActivity 之舊 session 不殺（與 HTTP middleware 一致）。upgrade 時亦查 idle——否則已逾時 session 仍可開控制通道 60s。client 見 4001 → 不 retry（retry 必 401）、redirect login |
| idle timeout 30 分硬編碼於 `createApp` default（`AppOptions.idleTimeoutMs` 注入，非 env 變量） | `auth-server/src/app.ts` | plan 已許「30 分鐘為硬編碼——可後續改 env 變量」；option 注入足供測試（小值＋real timers，免 fake-timer 與 file-store TTL 糾纏）。操作者欲調時再升 env |
| robot footprint 輪廓之螢向待操作者目驗（hardware-verify） | `web-client/src/map_render.ts`、`components/shared.tsx` | `footprintScreenRect` 之 length→縱（heightPx）、width→橫（widthPx）由單測 vector 鎖死，odom mode 旋向沿箭之 transform。然「式自洽」非「式合世」——須實機目驗：TB3 Waffle（0.281×0.306，**寬大於長**）之輪廓於圖中長橫比是否正（橫略長於縱）、odom mode robot 旋時輪廓隨箭同旋之向。反者翻 map_render 對應軸（單測 vector 同步改）。鄰於既有 minimap screen-direction 條 |
| service worker 之實機行為待驗（jsdom 無 serviceWorker） | `web-client/vite.config.ts` | vite-plugin-pwa `injectRegister:'auto'` 自注冊（`dist/registerSW.js`），手寫 `sw_register.ts` wrapper 已除；`npm run build` 出 `dist/sw.js`＋`workbox-*.js`＋`registerSW.js` 已驗。然 jsdom 無 serviceWorker API，**真機未驗**：(1) 重訪確由 cache 即開、(2) 弱網確達 login 殼、(3) 實時流（WS upgrade／WHEP POST／WebRTC video）確毫不經 SW（denylist＋runtimeCaching 唯 GET /assets/ 由設計保證，然須實機確 teleop 不斷）、(4) autoUpdate 確於 redeploy 後更殼。驗法：TROUBLESHOOTING「UI still shows the old version」條。`runtimeCaching` 之 `/^\/assets\//` anchor 於 same-origin full-URL 或不匹（precache globPattern 已涵 assets，故 redundant 而無害） |
| robot config 七鍵自 `.env` 遷 `config/robot.env`，經 `env_file`（非 `environment`）載入 | `docker-compose.yml`、`config/robot.env.example`、`.env.example` | operator 須經 web 設定頁改 robot identity/footprint/video source，然 `.env` 不 mount 入 container、且雜以 secret。故立 secret-free `config/robot.env`：teleop-server／video-bridge 以 `env_file: required:false` 載之（缺檔不崩），auth-server rw bind-mount `./config:/config` 俾 endpoint 原子改寫。**七鍵必自顯式 `environment:` map 移除**——否則 `environment` 優先序高於 `env_file`，shadow overrides 致改檔無效。secret（SESSION_SECRET 等）與 ROS/network/TLS 鍵仍居 `.env`，web tier 永不可及 |
| robot-config endpoint 採 partial PUT + merge（非全量覆寫） | `auth-server/src/routes/auth.ts`、`robot_config.ts` | PUT body 僅含已改之鍵亦可；validate 只驗 provided 鍵，過則與 `readRobotConfig` 既值 merge 後原子寫全七鍵。前端今送全七鍵，然 partial 容忍令 endpoint 對未來局部更新 robust；嚴格 allowlist 仍拒未列鍵（含 secret），故無洩漏面 |
| namespace localStorage（`pocket-teleop.robot-namespace`）退役，併入 server-backed Robot 區 | `web-client/src/components/SettingsDrawer.tsx` | 舊 Connection 區之 namespace input 自讀自寫 localStorage，全 codebase 無他者消費——真正顯示之 namespace 來自 server status message（`bridge.robotNamespace`），故該 localStorage 為死碼。今以 Robot 區（GET/PUT /auth/robot-config）為單一事實源，ROBOT_NAMESPACE 隨七鍵一併存於 `config/robot.env`，退役 localStorage 不改任何行為 |
| live runtime apply 暫緩，先出 restart-required 提示；前端「重起」鈕駁回 | `web-client/src/components/SettingsDrawer.tsx`、計劃 §風險 | 改 `config/robot.env` 後須重起 teleop-server／video-bridge 方生效。**前端重起鈕駁回**：須令 web-facing container 控 Docker（mount `docker.sock`／privileged sidecar），等同授其 host-root，乃全 stack 最劣 blast radius。故 Save 後僅顯「restart the stack to apply」提示，operator 自行 `docker compose ... up -d`。正解為 live apply（`teleop_node` param callback + 重建 pub/sub），列後續計劃取代提示，非重起鈕 |
| 任務二 Haiku cwd 釘主 repo，commit 落 main；controller cherry-pick 入 chain + reset main | 流程（subagent-driven-development） | 既知 gotcha #79/#101 復現：T2 子代理雖令 cwd 為 worktree，仍違禁執 `git commit`，且 cwd 釘 `/home/chngyicheng/pocket-teleop`（主 repo）——完整 commit（含 doc）落 `main`（基於 7632c98，未含 T1 之 compose 管路），worktree 僅得部分未 commit subset。救治：棄 worktree partial、`git cherry-pick` 完整 commit 入 `feat/env-settings`（無檔重疊，clean）、`git reset --hard origin/main` 復原 main。教訓重申：subagent 不執 git；派後必驗主 repo 與 worktree 二處 `git status` 與 log |
| SettingsDrawer 高度用 `100dvh`（非 `100vh`） | `web-client/src/components/SettingsDrawer.tsx` | 操作者報手機上 drawer 不能捲至底（Save 鈕被截）。`100vh` 用 layout viewport（含瀏覽器 address/nav bar 之高），較 visual viewport 為高，故 drawer 底落於 chrome 之下，內層 `overflow:auto` 無從捲及。Robot 區增高後始顯此患。`dvh`（dynamic viewport height）追 visible 區，修之。現代手機瀏覽器（Safari 15.4+/Chrome 108+）皆支援，teleop phone app 可恃 |
| robot-config UI 分二區（Video／Robot），各以獨立 **partial PUT** 存 | `web-client/src/components/SettingsDrawer.tsx` | 操作者問：頂 Video 區（runtime MediaMTX source 切換，per-browser、即時）與底 VIDEO_TOPIC（server config）關係不明。二者本異層，混置一區易惑。故將 `VIDEO_TOPIC`/`VIDEO_TOPIC_TYPE` 自 Robot 區移入 Video 區之「ROS2 Camera」子組，附註「Source=ROS2 時用此 topic」；Robot 區僅留 identity+footprint。二區各一 Save，各送 partial PUT（endpoint merge server-side，T2 已支援），不相覆。共用一份 mount 時 GET 之 robotConfig state；fieldErrors keyed by field 共用，save-result toast 各一。restart-required 提示文字與「ROS2 Camera」靜態註解皆含 "restart" → 測須以精確字串斷成功 toast，免 multi-match |
| scan 疊置採方案 A1（scan 配 capture pose、map 仍最新 pose），絕對延遲不補 | `web-client/src/map_render.ts`、`server/src/teleop_node.cpp` | 二層皆 robot-centered、front 上，疊合之充要：置 map 之 pose ＝ scan capture 時之真 pose。今 scan 附 capture pose，client 以之轉 world 再經最新 mapPose 投屏——scan 落於 map 牆（相對 desync 消），robot marker 仍居最新 pose（新鮮）。棄方案 A2（map+scan 共 capture pose）：A2 雖亦對齊然 marker 滯後真實 robot 一延遲；teleop 首重障礙對齊牆而 marker 位新鮮。scan 之絕對延遲猶在然不可見（疊牆即足）；操作者若要 marker 亦無延遲須別解 |
| scan 疊置之螢向待操作者目驗（hardware-verify） | `web-client/src/map_render.ts`、`server/src/teleop_node.cpp` | `worldToScreenPoint`／新 `scanToScreenPoints` 之 world→screen 符號已由單測 vector 鎖死（且已驗與 `mapToScreenTransform` 同式，故 scan 與 map 共座標），`capturePose===currentPose` 還原舊 robot-centered（回歸鎖）。然「式自洽」非「式合世」——須實機目驗：robot 原地旋轉與直行時 scan 點確疊於 map 牆而無拖尾；frame 由 map↔odom 轉換時無誤投／無跳。鄰於既有 minimap／footprint screen-direction 條 |
| scan capture pose 用 tf-at-stamp，查敗則 fallback／略（client 退回今行為） | `server/src/teleop_node.cpp` | capture pose 以 scan 之 `header.stamp`（非 `TimePointZero`）查 tf 以時對齊；map frame 優先、odom fallback（與 `broadcast_pose` 同序）。擷取瞬之 transform 或未入 buffer（extrapolation）→ catch 後 `std::optional` 留 nullopt，略 pose 四欄，client 見無 pose 則退回 robot-centered（`selectScanCapturePose` 回 currentPose）。不致崩，唯該幀無補償。tf 路徑依 buffer 難純測，gtest 僅鎖 `build_scan_message` 組裝 seam；tf 正確性俟實機 |
| scan frame-match 守衛：scan.pose.frame ≠ mapPose.frame 則棄 capture pose | `web-client/src/map_render.ts`、`components/shared.tsx` | map↔odom fallback 切換瞬，scan.pose.frame 與 mapPose.frame 可能短暫不符。若以異 frame 體系之 capture pose 投 scan 則誤置。`selectScanCapturePose` 於 frame 不符（或 pose 缺）時棄 capture pose、以 currentPose 充——退回 robot-centered（可接受之瞬態），免跨 frame 誤投。非安全患，純顯示守衛 |
| disconnect 參須同時加於 Dockerfile CMD 與 `teleop.launch.py` 兩處 | `Dockerfile` CMD、`server/launch/teleop.launch.py` | 生產容器經 Dockerfile CMD `ros2 run ... -p` 啟動，**非** launch 檔；唯改 launch 則生產無效。倣既存遺漏路徑（ROBOT_NAME 等）兩處皆寫 |
| `DISCONNECT_ACTION*` 於 compose `environment:` 用非空 fallback（`:-stop`/`:-0`/`:-/return_home`），非 `:-` 空串 | `docker-compose.yml` teleop-server | launch 檔 `ParameterValue(EnvironmentVariable('DISCONNECT_ACTION_PARAM'), value_type=int)` 遇空串崩潰；compose 顯式列各 env，故須保證非空。Dockerfile CMD `${VAR:+}` 亦賴此恆為真 |
| `disconnect_action_param` 於 Dockerfile CMD 之 `-p` 不加引號（string 參皆加引號） | `Dockerfile` CMD | int 參值無空格、ros2 須見裸 `key:=0` 方推 int；加引號則同 string 模式無害但無益，留裸值表型別意圖 |
| ReturnHome 經注入 `return_home_callback` 觸發，非 TeleopServer 直調 ROS service | `server/include/teleop_server.hpp`、`server/src/teleop_node.cpp` | Local Contract：TeleopServer 不得含 rclcpp（須無 ROS2 編譯/測試）；service client 屬 TeleopNode，以 `std::function<void()>` 注入跨層 |
| network 丟包以「未答 ping 被新 ping 覆蓋」計，棄 plan 所定 pong sequence number | `web-client/src/teleop_client.ts` | protocol ping/pong 為 `{type:'ping'}`/`{type:'pong'}`，無 seq 欄，加之須改 server 兩端。既有 keepalive 為單 in-flight ping（`pingSentAt` 單值，新 ping 覆未答者即 `missedPongs` 增之點），於該點 push `pingWindow` false（丟）、pong 達 push true，正合單 in-flight 模型；20-ping 窗 lossRate 足以驅 4 格指示，無須協議擴充 |
| SIG 點擊展開詳情（rtt/jitter/loss popover）暫緩，改 `title` attr hover tooltip | `web-client/src/components/shared.tsx`、`views/MissionControl.tsx`、`MissionTablet.tsx` | plan §任務三步三定點擊展開細節面板，然其須新 overlay 狀態＋佈局，於三 SIG 位各置風險高；細節值已由 `SignalBars` 之 `title`（RTT·Jitter·Loss）懸停可見，且 LAT readout 與後續 latency graph 另呈。core value（預判降級之 4 格條）已達，popover 列後續 |
| network warn/danger 不另置浮動文字 badge，併入 SIG readout 之 tier 顏色 | `web-client/src/network_readout.ts`、`views/MissionControl.tsx`、`MissionTablet.tsx` | plan §任務四定「網絡差/網絡危」獨立 badge，然 codebase 既有 BAT readout 以 tier 變色（ok 綠/warn 琥珀/danger 紅）示降級，沿之一致性勝於新增浮層。`networkReadoutModel` 出 tier，SignalBars bar 色隨之（0→danger 紅、1–2→warn 琥珀、3–4→ok 綠、null→muted 灰），降級即見色變，無須另闢 badge 元素 |
| input arbitration 採「現役源主之」（active-source）而非嚴格靜態優先 | `web-client/src/teleop_client.ts` | 嚴格靜態優先（priority 高源恆壓低源）令僅配對而未動之 gamepad（搖桿歸中、每幀送零）即殺觸屏——操作者插上手柄卻不能用觸屏。改以「於 `ACTIVE_WINDOW_MS`(400ms) 內曾發 accepted 輸入方為現役」之 owner 概念，且連而閒之 owner 每送零即釋 ownership（活在零入分支：owner 之零照送並 `activeSource=null`）→ 閒置高源不鎖低源。取捨：持桿而靜（無新事件）之 owner 逾窗失 ownership，高源微漂可奪控；以 400ms 窗緩之（與既有 IDLE_MS 同感），且 publisher 仍以末 accepted 值續送，窗內無控制空窗 |
| 低源之零入不停高源之動（非 owner 之零被棄） | `web-client/src/teleop_client.ts` | sendTwist 零入分支：唯 `source === activeSource` 之零照送（觸發既有 zero-burst 停車並釋 ownership）；他源之零即 `return` 棄之。否則 touch 鬆指（送零）會停 gamepad 之續動——二源相絆。故零之「停車權」亦歸現役 owner 獨有，與非零之「驅動權」同源 |
| HUD inputSource 由 arbiter 之 `onInputSource` 驅動，去 bridge 內 recency 推算 | `web-client/src/hooks/useTeleopBridge.ts`、`web-client/src/teleop_client.ts` | 舊 bridge 以 `lastGamepad/lastTouchActivity` 時間戳 + 150ms interval 推 inputSource，與 arbiter 之真 owner 二套真相易歧（被棄之 touch 仍誤顯 'touch'）。改：TeleopClient 於 `activeSource` 變時發 `onInputSource(source|'idle')`（owner 釋為 null 即 'idle'），bridge 直取之為單一真相。去 recency refs/常數/idle interval。注：源不送零而徑止（不釋）則 HUD 滯留該源——實務中 gamepad 歸中與 touch 鬆指皆送零，故恆歸 idle |
| velocity slew-rate limiter 置於 publisher，sendTwist 不即時送（僅設 target） | `web-client/src/teleop_client.ts` | 操作者求平滑：大速度驟令致機器人 jerk。於 20 Hz publisher 限步斜坡 `currentTwist`→`targetTwist`（accel 0.5s/decel 0.2s），sendTwist 遂改為僅設 target，**去既有 immediate sendScaledTwist**——publisher 獨掌送出，免雙送與斜坡相左。代價：首令較舊即時送遲 ≤50ms（一 tick），然斜坡本意即漸進，無感。`repeatTwist`/`zeroFramesLeft`/`STOP_REPEATS` 退役，decel 斜坡代 zero-burst（抵零之 tick 送一 terminal 0 後靜默） |
| accel 緩（0.5s）decel 急（0.2s）非對稱；reversal 經零 | `web-client/src/teleop_client.ts` | 操作者定 decel 須較 accel 快（停車宜促、亦較安全）。`rampAxis`：唯「同向且 |target|>|current|」或 current=0 為 accelerating（用 accelStep），餘皆 decelStep——故由正轉負時，先以 decel 煞至 0（sign 異 → 非 accelerating），過零後 current=0 始 accel 入反向。step 由 publishIntervalMs 算（tick-rate 無關）。E-STOP `engageEstop` 即令 current+target 歸零（**bypass 斜坡**），機器人立停無 decel 尾 |
| keyboard default velocity 0.5 → 1.0（一鍵即全速，不經 analog shaping headroom） | `web-client/src/keyboard_handler.ts` | 操作者報鍵控龜速且 max-speed 似無效。因 KeyboardHandler 固定送 0.5，再經 `shapeAxis`（analog stick 之 deadzone+cubic）：shapeAxis(0.5)≈0.088，落於 cubic 受壓段 → published ≈0.088×cap（爬行）；max-speed 雖仍乘之，然基數太小故倍增亦微。keyboard 為 digital（按即走），宜全速：velocity 1.0 → shapeAxis(1)=1 → ×cap = 配置上限。analog 細控本屬 stick，非 keyboard |
| HUD VELOCITY bars/V·ω 取 `publishedTwist`（onPublish）覆全源，去 `gamepadActive ? gamepadTwist : localJoystick` heuristic | `web-client/src/hooks/useTeleopBridge.ts`、`web-client/src/views/MissionControl.tsx`、`MissionTablet.tsx` | 操作者：三源既皆匯於單一 publish 點，VELOCITY 條與底值應顯該點之**實際輸出**，非按源猜。舊法 gamepad 顯 gamepadTwist、touch 顯本地 joystick 位、keyboard 無顯（缺讀數）。改：publisher 每 tick 發 `onPublish` 之真實斜坡值 → bridge `publishedTwist` → 三 disp 軸直取之，全源一致且現斜坡。`gamepadTwist`（onTwist，gamepad-only）留作 on-screen joystick knob-follow（顯手柄位於觸屏搖桿），與 VELOCITY 讀數異職故並存 |
| nav goal 用 `rclcpp_action` `NavigateToPose` client，非 publish `/goal_pose` topic | `teleop_node.cpp` | nav2 核心不可靠訂 `/goal_pose`；RViz「Nav2 Goal」實呼 `/navigate_to_pose` action。action client 為唯一 baseline 且原生支援 cancel |
| pause/resume = `async_cancel_goal` 留存 goal ＋ resume 重送（nav2 自當前位重規劃） | `teleop_node.cpp` 狀態機 | nav2 核心無逐任務 pause/resume（open FR ros-navigation/navigation2#5213）；cancel+resend 為慣用 workaround，非「精確續軌」但對障礙已變場景更穩 |
| E-STOP 取消 nav goal **並清留存** goal | `teleop_server.cpp` estop 分支 → `nav_cancel_callback_` | 防 E-STOP 後 Resume 誤再驅自主移動；twist 歸零僅停 teleop 自身，cancel 經 nav2 controller 減速停（非瞬停）——真急停倚硬體 E-STOP |
| goal `frame_id` 由 env `NAV_GOAL_FRAME`（預設 map）server 蓋，client 不送 frame | `teleop_node.cpp`、`teleop.launch.py` | frame 為部署級決策（須對齊 nav2 global frame），非每-goal UI 選項；同 MAP_FRAME 之 .env 級制 |
| `NAV_ACTION` 僅經 `config/robot.env`（env_file），**不**入 compose teleop-server `environment` | `docker-compose.yml` | compose `environment` 覆 `env_file`；置之則破 web UI 編輯（同現 ROBOT_NAME/VIDEO_TOPIC）。偏離計畫「NAV_ACTION 亦於 environment」之注——該注會 shadow UI 值 |
| `Dockerfile` builder + runtime 皆 apt 裝 `ros-humble-nav2-msgs` | `Dockerfile` | 此 image 手裝依賴（無 rosdep）；`nav2_msgs` 不在 `ros:humble` 基底——缺則 colcon config 失敗（crash 後查得之 blocker） |
| 八鍵遷移：既有 `config/robot.env` 無 `NAV_ACTION` 行 → 空 → server 用 launch 預設 `/navigate_to_pose` | `robot_config.ts`、`robot.env.example` | 向後相容；舊部署無須改 robot.env 即運作（標準 nav2 action 名） |
| waypoint loupe 放大鏡延後 | `shared.tsx` MiniMapView（ponytail 註） | 純視覺、jsdom 不可測、計畫列為俟實機；展開 minimap 夠大可直接置點。實機調觸控精度時再加 |
| waypoint heading dial = RViz-style 絕對指向（拖向何處即朝何處，與拖距無關） | `map_render.ts` `pointerToWorldHeading`/`worldHeadingToScreenDeg`、`shared.tsx` MiniMapView | minimap map mode 為 base_link-fixed view（螢幕上 = robot forward = `mapPose.heading`）；初版誤用 `atan2(dy,dx)+heading`（standard math convention，忽略 screen-up=forward 與 y-flip），arrow 方向亦反號——goal heading 與使用者所見不符。修正：world heading = `mapPose.heading + atan2(mk.x-fx, mk.y-fy)`（純方向），arrow rotate = `(mapPose.heading - heading)`；10 unit test 釘往返 + 方向。position 本即 map frame（`screenToWorldPoint` 用 map-frame `mapPose`，server 蓋 `goal_frame=map`）——僅 heading 需修 |
| nav goal/path/marker 螢幕方向 + nav2 TF 一致性為 hardware-verify | nav 全鏈 | action send/cancel、狀態機、global path 黏合、frame 對齊俟真 nav2 stack 驗（gtest 僅釘 parse + build_goal_pose + decimate_path 純函式） |
