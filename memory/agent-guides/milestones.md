# Milestones — full history

> Full shipped history (one row per milestone). AGENTS.md keeps only a "Most recent" three-line summary in its "Milestones + deviations" section; every milestone's full row lives here. Tests column = webclient / auth / video-bridge / C++ (— where not yet present). Rationale per milestone: [deviations.md](deviations.md) + the plan linked in the AGENTS.md document index.

| Milestone | Tests (web/auth/vb/cpp) | Tag |
|---|---|---|
| Server — ROS2 WebSocket, command handler, teleop node | — | `v0.1.0-server` |
| Web client v0.1.0 — protocol, connection, gamepad, teleop client | 10 | `v0.1.0-client` |
| Practical gaps — gamepad profiles, reconnection, calibration UI | 43 | `v0.2.0` |
| Frontend UI — settings.ts, onTwist, responsive rewrite | 43 | `v0.3.0` |
| Touch joystick + polish — TouchJoystick, namespace, gamepad switch | 60 | `v0.4.0` |
| v0.5.0 — KeyboardHandler, retry+onPong, axis remap, input bar | 63 | `v0.5.0` |
| Video streaming — mediamtx, video-bridge, WhepClient, /video proxy, WebRTC panel | 85 / 31 / 19 | `v0.6.0` |
| Video source picker — /mediamtx-api proxy, VideoSourcePicker, settings UI | 99 / 34 / 19 | `v0.7.0` |
| v0.8.0 control reliability — key-up instant trigger, e-stop button+space, calib Ready | 103 / 34 / 19 | `v0.8.0` |
| v0.9.0 feedback — RTSP validation, stream-health badge, latency display | 117 / 34 / 19 | `v0.9.0` |
| v0.10.0 telemetry — odom subscribe/broadcast, onOdom, panel + compass | 119 / 34 / 19 | `v0.10.0` |
| Apply-button e2e verify — mediamtx-test container + 3 integration tests | +3 integ | — |
| v0.11.0 video sources — UDP/SRT/MJPEG validate/buildMtxSource/apply, MJPEG img direct | 149 / 34 / 19 | — |
| v0.11.0 review fixes — 5 logic defects + 8 tests | 157 / 34 / 19 | — |
| Auth bugfixes — account inline error, visibilitychange guard, healthcheck | 157 / 34 / 19 | — |
| location.replace fix + README | 157 / 34 / 19 | — |
| start.sh + mjpegImgEl typo + vitest file serialization | 157 / 34 / 19 | — |
| Whole-repo review — 6 Haiku fixed 30 findings (LAN exposure closed, backoff, video_bridge Lock, auth hardening, +28 C++ tests) | 157 / 34 / 19 / 40 | — |
| Mission Control UI integration — vanilla shell, trophy TDD, E-STOP z10, Space wired | 191 / 34 / 19 / 40 | `feat/mission-ui` |
| Mission UI React Phase 0+Wave 1 — React 18 + Vite 5 + jsdom + RTL; shared.tsx, hooks, SettingsDrawer; setup.ts polyfills | 244 / 34 / 19 / 40 | `feat/mission-ui-react` |
| Mission UI React Wave 2 — MissionControl phone + MissionTablet grid; axis-map; E-STOP z10 | 259 / 34 / 19 / 40 | `feat/mission-ui-react` |
| Mission UI React Wave 3 — App.tsx matchMedia switch, main.tsx createRoot, index.css palette, 9 integration tests | 268 / 34 / 19 / 40 | `feat/mission-ui-react` |
| Mission UI design-parity — Math.hypot V/ω, atan2 heading, LAT from latencyMs, portrait telemetry, static readouts | 264 / 34 / 19 / 40 | `feat/mission-ui-react` |
| Mission UI smoke fixes — tablet breakpoint 700 (Fold 6), right SettingsDrawer, font harmonize, zone joysticks; branches merged | 264 / 34 / 19 / 40 | `feat/mission-ui-react` |
| WebRTC e2e fix — video-bridge target:runtime, sleep(inf) crash, RTSP→RTMP, ICE pin, WHEP pathRewrite; root cause host ufw DROP UDP 8891 | 264 / 34 / 19 / 40 | — |
| BUG 1 stop-on-release (SAFETY) — TeleopClient 20 Hz continuous publish + bounded zero-burst; axesRef cross-axis fix | 275 / 34 / 19 / 40 | `feat/control-safety-fixes` |
| BUG 4 latching E-STOP (SAFETY) — estop/estop_reset/estop_state protocol; C++ latch; engage/reset; ENGAGED banner | 286 / 34 / 19 / 44 | `feat/control-safety-fixes` |
| BUG 3 tablet top-bar — `■ STOP` label, robot-name sole shrink target, E-STOP pinned flexShrink:0 | 290 / 34 / 19 / 44 | `feat/control-safety-fixes` |
| BUG 5 video telemetry — WhepClient getStats@1Hz fps/res; fake UP/BAT/SIG → `—` | 300 / 34 / 19 / 44 | `feat/control-safety-fixes` |
| Settings drawer UX — slide-left, topOffset, burger toggle, backdrop scrim, Mission palette | 306 / 34 / 19 / 44 | `feat/settings-drawer-redesign` |
| Post-merge bugfixes — offline vendored woff2, auth theme + /auth-static route, enforceDefaultCredentialChange | 343 / 46 / 19 / 44 | `fix/post-merge-bugs` (merged `7bb948f`) |
| Collapsible rails + video fit — CollapsibleRail slide-out bookmark, toggle z15 > joystick z5, objectFit contain; portrait unchanged | 323 / 34 / 19 / 44 | `feat/collapsible-rails-video-fit` (merged `00f14b4`) |
| Video lazy-load + NO SIGNAL + gzip first-paint — 甲 useWhepStream defers start (requestIdleCallback) + code-splits WhepClient; 乙 VideoSignalOverlay (CONNECTING…/RECONNECTING…/NO SIGNAL, z2 < joystick z5); 丙 `/perf` beacon (Navigation/Paint/resource timing → auth log); 丁 instant `#boot-splash` in index.html; **戊 root cause: nginx gzip was off** — new `nginx.conf` (`gzip on` + immutable /assets cache), bundle 186→57 KB, `readyMs` 7919→~1937 ms | 363 / 49 / 19 / 44 | `feat/video-lazy-load` (merged `85ad697`) |
| Adversarial backlog cleared — 6 hardening fixes: protocol odom `Number.isFinite` guard, MissionControl LAT readout (neg/NaN/Inf → `— ms`), Space-key E-STOP ignored while editable field focused, TeleopClient `maxMissedPongs` zombie-link detector (onClose + reconnect), auth change-password rejects new==current (400), **/ws upgrade now session-authenticated** (`makeWsUpgradeHandler` runs express-session, fail-closed); pong-timeout test rewritten to standard `vi.mock(connection)` harness | 372 / 51 / 19 / 44 | — |
| Reconnecting counter wired live — top-bar chip showed hardcoded placeholder `⟳ Reconnecting… (3)`; now reads `bridge.retryCount` (counts up per attempt) in MissionControl + MissionTablet (`connText`); static `CONNECTION_LABELS.reconnecting` placeholder number dropped | 373 / 51 / 19 / 44 | — |
| Header robot identity de-faked — top-bar showed hardcoded `bot-07` / `ns/robot1`; now `robotLabel = robotName \|\| robotType` (falls back to robot model), renders nothing when both empty; namespace span only shown when non-empty (MissionControl + MissionTablet) | 375 / 51 / 19 / 44 | — |
| Settings drawer z-index fix — drawer panel z17 / backdrop z16 now above CollapsibleRail toggle tab (z15); previously z9/z8 let rail tab paint over the drawer | 375 / 51 / 19 / 44 | — |
| Dedup HUD primitives — `fallow` flagged MissionControl/MissionTablet clone groups; extracted `Crosshair` (16×16 line cross) + `JoystickZone` (positioned bottom-corner Joystick wrapper) into `shared.tsx`. MissionControl drops 2 inline crosshairs + 4 inline joystick wrappers (landscape + portrait branches), MissionTablet drops 2; behavior-preserving, inner Joystick keeps `data-testid="joystick-zone"`. Tablet's SVG reticle left as-is (different design). +7 shared tests | 382 / 51 / 19 / 44 | — |
| Two webclient reds fixed — `whep_client` "clears safety timeout" test was broken (fake PC defaulted `iceGatheringState='complete'` → no timer armed; fired wrong `onconnectionstatechange` event; `addEventListener` was a no-op `vi.fn`). Reworked mock: gated `_initialIceGatheringState`, functional listener registry + `_setIceGatheringState` dispatch; assert via `vi.getTimerCount` (source unchanged — was already correct). `integration.test.ts` now probes the server once + `describe.skipIf(!serverReachable)` (11 skip without live stack, no 20 s hard-fail) | 382 / 51 / 19 / 44 | — |
| Dead-export cleanup — fallow flagged 4 unused exports; all used internally so dropped only the `export` keyword on `PUBLISH_INTERVAL_MS`/`STOP_REPEATS` (teleop_client) + `CollapsibleRailProps` (CollapsibleRail); `ConnectionState` (shared) wired into useTeleopBridge replacing its 2× inline duplicate union (TeleopBridge field + useState generic). Type-only, no behavior change | 382 / 51 / 19 / 44 | — |
| Gamepad controls improvements — 6 serial worktree tasks (Haiku + trophy TDD): **務A** bridge exposes `gamepadTwist`+`inputSource` (activity-timestamp, idle reversion); **務一** `input_shaping.ts` deadzone 0.1 + cubic curve at `sendTwist` choke (covers gamepad/touch/keyboard; repeatTwist stores shaped-normalized so a held stick never self-zeros); **務三** STANDARD axes remapped — rotate(az)←left-stick X (axis0), strafe(ly)←right-stick X (axis2), lx unchanged (DualShock untouched); **務五** E-STOP on LB (button 4) cross-source toggle (Xbox/GameSir/Generic; `handleGamepadButton` engages/resets the shared latch; button-exists guard); **務二** Joystick `externalValue`/`externalActive` — knob follows gamepad truth, hint hides, touch still wins; **務四** `setMaxSpeed` scales at send choke (default 1.0), `SpeedStepper` +/- in left-rail SPEED panel, persisted to localStorage (hard caps lin 0.1–2.0 / ang 0.1–3.0), no socket reconnect on change | 451 / 51 / 19 / 44 | `feat/gp-*` |
| Gamepad post-merge UX fixes — **(1)** held-stick regression: `GamepadHandler` polls via `requestAnimationFrame` (not bare `setInterval`) so a held stick reads fresh (Chrome refreshes `getGamepads()` only on the rAF loop) and republishes ~20 Hz; **(2)** published-velocity surfaced — VELOCITY bars + tablet V/ω readout now read the active source and show actual `cmd_vel` (shaped × cap), not raw stick; **(3)** left rail reordered VELOCITY→SPEED→ODOMETRY→VIDEO so the SPEED ± sit clear of the bottom joysticks (drawer kept at z1 — not raised); **(4)** `SpeedStepper` restyled to Mission palette, single line, label-left / `− x.x unit +` right, columns aligned; **(5)** common joystick sizing (base 120 / knob 50); **(6)** long-phone-landscape (aspect ≥ 17/10) uses a smaller joystick (150/90/38, no clip → no slice) so the rail scrolls above it, Fold keeps full 200 both orientations | 451 / 51 / 19 / 44 | — |
| Docs refresh + README rewrite — professional README (SVG banner in UI font, screenshot placeholders, controls table, badges); troubleshooting moved to `TROUBLESHOOTING.md` (incl. Brave fingerprinting + held-stick + cmd_vel hz check) | 451 / 51 / 19 / 44 | — |
| SLAM minimap — 6 serial chain-branch tasks (Haiku + trophy TDD): **務A** protocol map/pose/scan parse + `map_codec.decodeRle` (trinary `u3f2o1` RLE); **務一** C++ `/map` sub (transient_local+reliable), `map_codec` crop window (24 m default) + RLE encode, 1 Hz timer gated new-map/moved-2m/5s-late-joiner, `broadcast` returns delivery bool (undelivered map stays dirty); **務二** tf2 `map→base_link` pose @5 Hz (odom fallback, throttled warn) drives crop center, LaserScan sensor-data QoS decimated ≤120 pts @5 Hz with yaw frame-correction; **務三** TeleopClient onMap/onPose/onScan → bridge `mapGrid` (decode-once) / `mapPose` / `scan`; **務四** `map_render.ts` pure transforms (vitest vectors lock conventions) + MiniMap canvas (offscreen per map msg, smoothing off, null-ctx guard, Mission palette, MAP/ODOM/NO MAP label, fallback = old grid); **務五** views pass props, `navPose = mapPose ?? odom`, README/TROUBLESHOOTING/data-schema. Env passthrough for all topics/frames. Server live-verified on real robot | 548 / 51 / 19 / 69 | `feat/map-*`, `feat/pose-scan-server`, `feat/minimap-*` |
| HTTPS/TLS opt-in — 3 chain-branch tasks (Haiku + trophy TDD; controller reworked the Caddyfile): **務一** auth-server `trust proxy` + cookie `secure:'auto'` (Secure follows X-Forwarded-Proto; plain LAN HTTP unchanged; 2 new vitest) + `BIND_HOST` env (loopback pin behind the proxy); **務二** `--profile tls` host-network Caddy terminating 443/80 → reverse_proxy localhost:8080 (WS upgrade incl.), cert mode one knob — `TLS_ACME_EMAIL` set = Let's Encrypt, empty = `tls internal` (defaulting compose-side as `TLS_CERT_MODE` since Caddy's `{$VAR:default}` ignores empty-but-set), caddy-data/config volumes persist ACME account; **務三** README "Enabling HTTPS" (3-mode table, phone root-CA trust steps, BIND_HOST + ufw 443), data-schema BIND_HOST/caddy tables, live-verify deviation row. Side fix: video-bridge suite red since d4d19a9 (5 tests asserted old RTSP pipeline) — now assert flvmux/rtmpsink, RTMP doc corrections (video_bridge docstring, data-schema, repository-structure) | 556 / 52 / 20 / 69 | `feat/tls-cookie`, `feat/tls-caddy`, `feat/tls-docs` |
| Login rate limit — 1 task (Haiku + trophy TDD; controller de-duplicated dead express-rate-limit dep + res.locals round-trip): failed `/auth/login` counted per IP (10/min) and per username (5/min, X-Forwarded-For-aware via trust proxy); 429 + `Retry-After` + inline hint page; success consumes no quota; counters in-memory per app instance (restart resets — accepted, single-operator). Hand-rolled tracker: login failures are 302 redirects, status-based accounting in express-rate-limit can't see them (deviation row). 4 new vitest: 11th same-IP failure blocked, 6th same-username cross-IP failure blocked, successes exempt, Retry-After present | 556 / 56 / 20 / 69 | `feat/rate-limit` |
| Session idle timeout — 3 chain-branch tasks (Haiku + trophy TDD; controller added upgrade-time idle reject + sustained-activity heartbeat interval, de-placeholdered a frame test): **務一** auth-server 30-min idle destroy (`lastActivity` middleware after session load; `GET /auth/session-status` excluded from activity so the poll can't keep sessions alive), `GET /auth/session-status` (`{remainingMs}`) + `POST /auth/heartbeat`, cookie maxAge 30 d → 30 min (rolling), WS upgrade rejected 401 when idle-expired + per-connection session-store re-check every 60 s killing expired sockets with a hand-built 4001 close frame (`buildWsCloseFrame`; http-proxy-middleware pipes raw TCP — no ws close API), `idleTimeoutMs` injectable via `AppOptions`; **務二** web `useSessionStatus` (poll 60 s, warn < 5 min, throttled ≥60 s heartbeat on pointer/key/gamepad input incl. sustained-active interval re-check, 401 → login redirect) + `SessionBanner` bottom toast ("Stay logged in" → keepAlive); **務三** teleop_client treats close 4001 as terminal (onClose, no retry backoff), bridge redirects to /auth/login. Plan's "WS message = activity" dropped — the 200 ms keepalive ping would defeat the timeout (deviation rows). 22 new tests (8 auth + 14 webclient) | 570 / 64 / 20 / 69 | `feat/idle-timeout-server`, `feat/idle-timeout-banner`, `feat/idle-timeout-logout` |
| Robot footprint outline + service worker precache — 4 chain-branch tasks (Haiku + trophy TDD; controller renumbered C++ test ports off 19092, regenerated the package-lock for vite-plugin-pwa): **務一** C++ teleop_node params `robot_length_m`/`robot_width_m` (default 0.0) → `status` JSON `robot_length`/`robot_width` (always present, 0 = unconfigured), env passthrough `ROBOT_LENGTH_M`/`ROBOT_WIDTH_M` over the existing 4-file ROBOT_NAME road (3 gtest); **務二** `protocol.ts` status parse (finite-positive guard else 0) + `useTeleopBridge` exposes `robotLength`/`robotWidth`, `onStatus` extended at the tail (10 vitest); **務三** `map_render.footprintScreenRect` (ROS length=x→vertical, width=y→horizontal, 14 px zoom gate) + MiniMap dashed accent rect under the arrow / above scan (map mode axis-aligned, odom rotates with heading), all 3 MiniMap call sites wired (14 vitest); **務四** `vite-plugin-pwa@1.3.0` generateSW precaches the app shell (autoUpdate, navigateFallbackDenylist for /ws //video //whep //auth //perf //mediamtx-api, CacheFirst /assets/), thin injectable `sw_register.ts` (prod-only, swallows errors), nginx `/sw.js` no-cache; WS/WHEP/WebRTC never touch the SW (5 vitest, build emits dist/sw.js). Footprint screen-direction + live SW behavior are hardware-verify rows (deviations). TurtleBot3 Waffle dims: 0.281 × 0.306 m | 599 / 64 / 20 / 72 | `feat/footprint-*`, `feat/sw-precache` |
| Robot config settings page — 3 chain-branch tasks (Haiku + trophy TDD; controller cherry-picked a cwd-pinned T2 commit off main back onto the chain, polished the UI labels): **務一** secret-free `config/robot.env` overrides (7 keys: ROBOT_TYPE/NAME/NAMESPACE, ROBOT_LENGTH_M/WIDTH_M, VIDEO_TOPIC/TYPE) loaded by teleop-server + video-bridge via `env_file: required:false`, removed from explicit `environment:` so they don't shadow it, auth-server bind-mounts `./config:/config` rw; keys migrated out of `.env.example` into `config/robot.env.example`; **務二** `auth-server/src/robot_config.ts` (parse/serialize env-file, allowlist, per-field validate — ROBOT_TYPE∈{diff_drive,holonomic}, ROS name rules, finite 0–10 m, no newline/`=`/control chars; atomic temp+rename write) + `GET`/`PUT /auth/robot-config` (session-guarded, partial-PUT merge, 400 + per-field errors, never exposes secrets, `restartRequired:true`), wired via `AppOptions.robotConfigPath` (32 vitest); **務三** `SettingsDrawer` Robot section replaces the dead namespace-localStorage Connection section — 7 labelled fields loaded on mount, saved via PUT, "restart the stack to apply" notice on success, inline per-field errors on 400 (shared fieldLabel/fieldError styles). Live runtime apply deferred (restart-required banner instead; front-end restart button rejected — would need docker.sock = host-root); namespace localStorage retired (deviations) | 601 / 96 / 20 / 72 | `feat/env-settings` |
| Robot config post-merge fixes — operator feedback after first run: **(1)** migration gap — moving the 7 keys out of `.env` left existing deployments with no video / default dims; fix = seed `config/robot.env` from the old `.env` values (TROUBLESHOOTING entry added); **(2)** Settings drawer unscrollable on mobile — `100vh` → `100dvh` so the panel matches the visible viewport (bottom was under the browser chrome); **(3)** video controls regrouped — `VIDEO_TOPIC`/`VIDEO_TOPIC_TYPE` moved out of Robot into a "ROS2 Camera" subgroup under the Video Source picker, with a note that the topic applies when Source = ROS2; each section now Saves via an independent **partial PUT** to `/auth/robot-config` (merged server-side). +1 vitest (video-topic partial save) | 602 / 96 / 20 / 72 | — |
| Scan↔map sync (capture-pose overlay) — 3 chain-branch tasks (Haiku + trophy TDD): **務一** C++ `on_scan` looks up the robot pose at the scan's `header.stamp` (map frame first, odom fallback, omitted if both tf lookups fail) and emits optional `pose_x`/`pose_y`/`pose_heading`/`pose_frame`; JSON assembly extracted to pure `map_codec::build_scan_message` seam (3 gtest pin has-pose/no-pose shapes; tf path is hardware-verify); **務二** `protocol.ts` parses the four pose fields into structured `ScanPose` (frame ∈ {map,odom}; any missing/invalid field → pose undefined, scan never dropped), flowing TeleopClient.onScan → `useTeleopBridge` ScanData (8 vitest); **務三** `map_render` adds `worldToScreenPoint` + reworks `scanToScreenPoints(scan, capturePose, currentPose, …)` to project each ray base_link → world (via capture pose) → screen (via current map pose), matching `mapToScreenTransform`'s world→screen convention so scan lands on map walls; `selectScanCapturePose` falls back to current pose on frame-mismatch/absent (robot-centered), MiniMap wired; capturePose===currentPose regression-locked by vector tests (5 vitest). Relative desync removed (scan overlays walls); absolute scan latency remains but invisible. Scan-overlay screen-direction + tf-at-stamp behavior are hardware-verify | 614 / 96 / 20 / 75 | `feat/scan-map-sync` |
| Gamepad cold-start detection (SAFETY) — built 2026-06-07 (webclient 471 then), merged 2026-06-14 onto current main. 3 serial worktree tasks (Haiku + trophy TDD): **務一** `GamepadHandler` `attach()`/`detach()` bind `window` gamepadconnected/gamepaddisconnected (idempotent, SSR-safe), `poll()` centrally manages connection state for both event + warm-poll paths (`onConnectionChange`/`isConnected()`), rAF loop wraps `poll()` in try/finally so one throw no longer kills it; **務二** `TeleopClient` attaches+starts the handler in its constructor (detection now independent of the socket), `onClose`/`handlePongTimeout` → `setEnabled(false)` (stop sending, keep polling), `connect()`/`scheduleRetry` → `setEnabled(true)`, `disconnect()` → `detach()` — `Connection.send` already no-ops when not OPEN; **務三** `onGamepadConnected` option → `useTeleopBridge.gamepadConnected` → **🎮 GP** chip beside the connection chip in both views (only when connected, so touch-only phones unaffected). Root cause: detection was chained to the WebSocket + no `gamepadconnected` listener, so a cold browser missed the activation moment; a second page warmed the process-global gamepad service. Merge resolved 4 conflicts (AGENTS.md + 3 test files — both sides had appended tests at the same spot; kept all) + a stale GamepadHandler mock (added attach/detach to teleop_client_map_pose_scan). Operator cold-browser activation verify pending | 634 / 96 / 20 / 75 | `fix/gamepad-*` |
| Disconnect-after behavior (SAFETY) — 4 tasks (Haiku + trophy TDD; server-only plan, zero React staleness): **務一** `TeleopServer` gains `enum DisconnectAction {Stop,Hold,ReturnHome,Continue}` + string helpers, ctor params (action, param_ms, injected `return_home_callback`), `last_twist` cache in on_message, status JSON `disconnect_action`; watchdog branches — `stop` zero+close (unchanged default), `hold`/`continue` republish cached twist each tick for `param_ms` then single zero+close (estop while holding republishes zero), `return_home` invokes the callback then zero+close (null callback → stop). Fires once/session (9 gtest, separate ports 19093+); **務二** `teleop_node` ROS params `disconnect_action`/`disconnect_action_param`/`return_home_service`, `std_srvs/Trigger` client on `/return_home` created + callback wired (package.xml + CMakeLists std_srvs). **Post-ship operator decision: `return_home` auto-trigger DISABLED** — the node callback logs and behaves as stop; the Trigger client stays wired for a one-line re-enable. Node test asserts the service is NOT auto-called + still stops; **務三** `protocol.ts` status `disconnect_action` (default 'stop') → teleop_client.onStatus → useTeleopBridge → App → `SettingsDrawer` Robot section read-only line (12 tests); **務四** docs + wiring — controller added the params to **both** prod invocation paths (Dockerfile CMD `-p` flags AND `teleop.launch.py`; the launch file alone is dead in prod) + docker-compose `environment:` with non-empty fallbacks (avoids the launch int-parse empty-string crash). `hold`/`continue` flagged everywhere as fail-stop violations. Cold-browser hardware-verify N/A (server logic). | 646 / 96 / 20 / 86 | `feat/disconnect-behavior` |
| Battery telemetry — 4 tasks (Haiku + trophy TDD; chained off disconnect branch): **務一** `teleop_node` subscribes `sensor_msgs/BatteryState` on configurable `battery_topic` (`BATTERY_TOPIC`, default `/battery_state`; sensor_msgs already a dep), caches latest, 1 Hz wall-timer broadcasts `{type:battery, percentage(0–100), voltage, current, charging}` — no message = no broadcast (badge hidden), not 0% (2 gtest); **務二** `protocol.ts` parses battery (percentage/voltage/current `number|null` on non-finite, charging must be bool) → teleop_client.onBattery → useTeleopBridge `battery` + `batteryEstimateMinutes`, with a pure `battery_estimate.ts` (60 s sample window → discharge rate → remaining minutes; charging → null) (22 vitest); **務三** pure `battery_readout.ts` maps battery→`{value,tier}` ('—'/none, `<pct>%` tier >80 ok/20–80 warn/<20 danger, `⚡<pct>%` ok when charging), MissionControl (both readouts) + MissionTablet map tier→palette (20 vitest); **務四** controller wired `BATTERY_TOPIC` into both prod paths (Dockerfile CMD + launch) + compose env + .env.example, docs (data-schema battery msg/param/env, README, server/AGENTS). Estimate is in the bridge but not yet surfaced (detail panel deferred). Discovered + recorded pre-existing `tsc --noEmit` red in `useTeleopBridge.test.tsx` (vitest is the gate). | 688 / 96 / 20 / 88 | `feat/battery-telemetry` |
| Network quality indicator — 2 chain-branch tasks (Haiku + trophy TDD; client-only, fills the faked `SIG —` readout): **務一** pure `network_quality.ts` `computeQuality(stats)→0–4` (component scores RTT `<100/200/350/600`, jitter `<20/50/100/200`, loss `<1/5/10/20%` → 4/3/2/1/0, **min** aggregation; non-finite/negative → 0). `TeleopClient` tracks last-20 RTT samples (jitter = mean abs consecutive diff) + a 20-ping answered/lost window (lossRate via the existing overwrite-of-unanswered-ping point), exposed by `getNetworkStats()`; no protocol seq added, zombie-link/`missedPongs` behavior untouched (45 vitest); **務二** pure `network_readout.ts` quality→tier (null/non-int/out-of-range→none, 0→danger, 1–2→warn, 3–4→ok), `SignalBars` 4-bar pill in `shared.tsx` (fills bars `< quality`, muted otherwise, `title` tooltip = RTT·jitter·loss), `useTeleopBridge` polls `getNetworkStats()` at 1 Hz (gated by `hasNetworkDataRef` set on first `onLatency`) → `networkQuality`/`networkStats`, three SIG readouts in MissionControl (×2) + MissionTablet swapped to `SignalBars` with tier→palette color (20 vitest). Click-to-expand detail popover deferred (title tooltip instead); separate floating warn/danger text badge folded into the readout color tier (matches BAT convention). | 753 / 96 / 20 / 88 | `feat/network-quality` |
| Connection resume on foreground — client-side fast recovery for backgrounded tabs (the `/ws` socket + WHEP `RTCPeerConnection` live in the tab, so browser timer-throttling/page-suspend degrades both; on return they previously limped back through retry/backoff). **務一** framework-free logic: `Connection.isOpen()`; `TeleopClient.resume()` (mid-backoff → reconnect now, open → probe-ping so the zombie detector fires fast, closed → reconnect) with `reconnectNow()` extracted from `scheduleRetry` — controller caught + fixed a regression where the shared `reconnectNow` reset `retryAttempt` and flattened exponential backoff to a constant 5 s; reset now lives only in `resume()`, locked by 2 backoff-growth tests; `WhepClient.resume()` rebuilds the PC, `'disconnected'` connectionState gets a 2 s grace (was ignored until `'failed'`), fps-stall watchdog (`framesDecoded` flat 3 polls while live → rebuild a frozen-frame stream); `VideoStats`/`onStats` unchanged (11 vitest); **務二** `useTeleopBridge` + `useWhepStream` each add a `visibilitychange`→visible + bfcache `pageshow` listener calling `client.resume()` (4 vitest). Inline listener block duplicated across both hooks — intentional, not abstracted. | 768 / 96 / 20 / 88 | `feat/connection-resume` |
| Input arbitration (gamepad>keyboard>touch) — 3 chain-branch tasks (Haiku + trophy TDD; T1/T2 src, T3 keyboard re-wire). Gamepad + touch (and now keyboard) all funnel into `TeleopClient.sendTwist`, where the continuous publisher repeated whichever wrote last — two live sources alternately overwrote each other → robot jitter. **務一** arbitration at that single confluence point: `InputSource` type, priority gamepad(3)>keyboard(2)>touch(1), `activeSource`/`lastActiveAt`, `ACTIVE_WINDOW_MS=400`; a source is "active" if it sent accepted input within the window. Non-zero input acquires if owner idle/none, continues if same source, seizes if priority ≥ owner, else rejected (no send/onTwist/repeat); zero input is honored only from the owner (frees ownership) — a lower source's zero can't stop a higher source. `onTwist` now carries the accepted source; reset on connect/reconnect. A continuously-zeroing owner releases every frame, so an idle high source never locks out a lower one (11 vitest). **務二** touch routed through the arbiter (`sendTwist(...,'touch')`); HUD input-source unified — new `onInputSource(source\|'idle')` fires on owner change, `useTeleopBridge.inputSource` follows it directly (deleted the bridge's recency refs/idle interval; a rejected touch no longer mislabels the HUD), `gamepadTwist` updates only when `onTwist`'s source==='gamepad' (3 vitest). **務三** `KeyboardHandler` wired into TeleopClient like the gamepad handler (ctor build → start on connect, stop on disconnect, gate-off on close/pong-timeout), WASD/arrows at keyboard priority, now ignored while an editable field holds focus; integration test drives a real DOM keydown through a connected client (5 vitest). | 787 / 96 / 20 / 88 | `feat/input-arbitration-*` |
| Velocity slew-rate limiter + full-speed keyboard + unified HUD velocity — operator-driven control-feel fixes after the arbitration merge, done in-context (controller, trophy TDD; not subagent-spawned per operator request). All at the single `sendTwist`/publisher confluence point. **(1)** `KeyboardHandler` default velocity 0.5 → 1.0 so a keypress commands full configured speed (the old 0.5 hit the crushed part of the cubic shaper ≈0.088 → crawl, speed cap barely mattered). **(2)** Slew-rate (acceleration) limiter in the 20 Hz publisher: `currentTwist` ramps toward `targetTwist` (named consts ACCEL_TIME_MS 500 / DECEL_TIME_MS 200 → 0.1 / 0.25 per tick; per-axis; `rampAxis` uses accel step only when moving away from zero in-direction, else the sharper decel — so a reversal brakes through zero then accelerates reverse). sendTwist now only sets the target; the publisher owns the send and the decel ramp replaced the fixed STOP_REPEATS zero-burst (one terminal zero at rest, then silent). E-STOP zeroes current+target instantly (bypass). repeatTwist/zeroFramesLeft/STOP_REPEATS retired (8 new slew vitest; updated continuous-publish + speed-scale tests to ramp semantics). **(3)** new `onPublish(lx,ly,az,source)` → bridge `publishedTwist` → MissionControl/MissionTablet VELOCITY bars + numeric V/ω read the actual ramped cmd_vel for every source (gamepad/keyboard/touch), replacing the `gamepadActive ? gamepadTwist : localJoystick` heuristic (fixes missing keyboard readout; `gamepadTwist` still drives the gamepad knob-follow). 2 new bridge vitest + publishedTwist added to all view-test fake bridges. Runtime/Settings tuning of accel/decel deferred. | 797 / 96 / 20 / 88 | `feat/vel-ramp-1` |
| MiniMap expandable + portrait reposition (operator-driven; initial subagent task + 3 in-context follow-ups). Refactor: internal `MiniMapView` (render/refs/pinch/tap/wheel + `hidden` prop) split from a thin `MiniMap` wrapper holding `expanded`. `expandable` (all 3 call sites) → tap opens a full-screen overlay `createPortal`-ed to `document.body` (must escape CollapsibleRail's `transform`/`overflow:hidden`; `position:fixed`, zIndex 200, bg forced translucent so video shows through); tap backdrop/map collapses; collapsed view `hidden` while expanded. Tap detection (`tapStartRef`, 1 pointer, <10 px, <400 ms) is unconditional (no-map ok); pinch + native non-passive wheel zoom gate on map mode (2-finger suppresses tap). `onExpandedChange` → while expanded the view raises JoystickZones (z 250) + E-STOP (z 260, `position:relative`) above the overlay for drive-with-map-open, and landscape/tablet close both rails (grid cols→0 = fullscreen) restoring prior state on collapse (`prevRailsRef`, keyed on `mapExpanded`). Portrait minimap moved bottom-center `bottom:204`, size 128. 11 new vitest. | 808 / 96 / 20 / 88 | `feat/minimap-expand` |
| Over-engineering cleanup (ponytail-audit, direct fix). **(1)** Deleted the dead battery runtime-estimate feature: `battery_estimate.ts` (`estimateRemainingMinutes`/`pruneSamples`) + the `useTeleopBridge` sample buffer/`batteryEstimateMinutes` plumbing + the `batteryReadoutModel` `estimateMinutes` param — the estimate was computed end-to-end but `batteryReadoutModel` ignored it and nothing rendered it. **(2)** Removed the hand-rolled `sw_register.ts` wrapper; vite-plugin-pwa now auto-injects registration via `injectRegister:'auto'` (`dist/registerSW.js`), same prod-only behavior. Deleted `battery_estimate.test.ts` + `sw_register.test.ts` + 1 obsolete `battery_readout` case. ~150 LOC + 2 files cut, no behavior change. | 790 / 96 / 20 / 88 | (this branch) |
| nav2 waypoint send (tap expanded minimap → goal) — 6 tasks (Haiku + trophy TDD, per-task worktrees; recovered after a mid-run crash left task 1 unbuilt — controller fixed 2 compile bugs + the missing `ros-humble-nav2-msgs` apt dep, the crash blocker). **務一** `CommandHandler` nav_goal/nav_pause/nav_resume/nav_cancel (finite guards) + variant; `TeleopServer` dispatch + four injected callbacks + **E-STOP cancels & clears the nav goal**; `TeleopNode` `NavigateToPose` `rclcpp_action` client + goal state machine (active/paused/stored, mutex-guarded), `build_goal_pose` pure seam, `nav_state` broadcast, `nav_action`/`goal_frame` params (18 parse + 2 build_goal_pose gtest). **務二** decimation pulled into pure `map_codec::decimate_path` (preserves endpoints, ≤64 pts) + on_nav_path subscribes nav2 `/plan` → decimated `nav_path` broadcast (3 gtest). **務三** config chain: `NAV_ACTION` is a UI key (robot.env + auth allowlist 7→8, VIDEO_TOPIC-style validation; NOT in compose `environment` — would shadow env_file), `NAV_PATH_TOPIC`/`NAV_GOAL_FRAME` are .env-level (compose + launch); launch wires all three. **務四** client transport: `map_render.screenToWorldPoint` (exact inverse, roundtrip-locked), protocol build×4 + parse nav_state/nav_path (shape+finite guards → unknown), teleop_client send×4 (goal+resume estop-gated, pause+cancel always), bridge `navState`/`navPath` + methods. **務五** UI: expanded-minimap waypoint placement (screen→world), marker + heading dial, nav2 path polyline, context control bar (Set→Send/Cancel; active→Pause/Stop; paused→Resume/Stop; Set gated on map frame), views wired (loupe magnifier deferred — ponytail). **務五乙** SettingsDrawer NAV_ACTION field. **務六** doc closeout. pause/resume = cancel + re-send stored goal (nav2 has no native per-task pause, FR #5213); cancel is a nav2 decel-stop, not a hard e-stop (hardware E-STOP remains the real one). Hardware-verify pending (real nav2 stack). **Follow-up fix:** the heading dial now aims RViz-style (world heading from the marker→finger direction in the base_link-fixed view) — the initial `atan2(dy,dx)+heading` used the wrong screen convention (+ arrow sign) so the sent heading didn't match the view; extracted `pointerToWorldHeading`/`worldHeadingToScreenDeg` pure helpers + 10 unit tests. | 853 / 98 / 20 / 106 | `feat/nav2-waypoint-*` |
